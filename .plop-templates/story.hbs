/* eslint-disable import/no-extraneous-dependencies */
import React from 'react';
import { storiesOf } from '@storybook/react';
// import { boolean, select } from '@storybook/addon-knobs';
import {{reactCase name}} from './{{reactCase name}}';

const stories = storiesOf('{{reactCase name}}s', module);

stories.add('Default {{reactCase name}}', () => (
  <{{reactCase name}}>
    Test Case
  </{{reactCase name}}>
  ),
  {
    info: {
      text: `
        {{component_desc}}
        `
    },
  }
)